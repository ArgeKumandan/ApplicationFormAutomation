@model ApplicationFormAutomation.WebUI.Models.Form

<form class="well form-horizontal" method="post" id="contact_form">
    <fieldset>
        <!-- Form Name -->
        <legend>@Model.Title</legend>
        <!-- Text input-->
        @{int index = 0;}
        @foreach (var item in Model.FormElements)
        {
            <div class="form-group">
                <label for="first_name" class="col-md-4 control-label">@item.Title</label>
                <div class="col-md-4 inputGroupContainer">
                    <div class="input-group">
                        <spain class="input-group-addon"><i class="glyphicon @item.GlyphIconCss"></i></spain>
                        <formElement form-element="@item" element-index-number="@(index++)"></formElement>
                      @*  <input id="@item.DataKey" name="@item.DataKey" placeholder="@item.Description" class="form-control" type="text">*@
                    </div>
                </div>
            </div>
        }

        <!-- Success message -->
        <div class="alert alert-success" role="alert" id="success_message">
            Success <i class="glyphicon glyphicon-thumbs-up"></i>
            Thanks for contacting us, we will get back to you shortly.
        </div>
        <!-- Button -->
        <div class="form-group">
            <label class="col-md-4 control-label"></label>
            <div class="col-md-4">
                <button type="submit" class="btn btn-warning">Kayıt <span class="glyphicon glyphicon-send"></span></button>
            </div>
        </div>
    </fieldset>
</form>